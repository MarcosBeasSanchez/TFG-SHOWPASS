spring.application.name=5-TFG
#mostrar Tablas y columnas
spring.jpa.show-sql=true
#create-drop
spring.jpa.hibernate.ddl-auto=create-drop

#tamano de las imagenes JSON subida por el ADMIN
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

#configuracion del email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=showpasstfg@gmail.com
spring.mail.password=ttvh gkxj ucga jluu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Direccion y puerto #Puerto HTTPS:8443
server.address=0.0.0.0
server.port=8080 

#protocolo HTTPS,SSL,TSL
server.ssl.enabled=false

# Ruta y tipo del archivo KeyStore (donde está el certificado)
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-type=PKCS12

# Contraseñas del almacén de claves y la clave privada
# NOTA: En un entorno real, estos deben ser gestionados por un secreto seguro (Vault, Kubernetes Secrets, etc.)
server.ssl.key-store-password=mi_contraseña_secreta
server.ssl.key-alias=alias-del-certificado

# --- H2 Database Console ---
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Permitir acceso desde navegador (sin restricción CSRF)
spring.h2.console.settings.web-allow-others=true

# Configuración de la base de datos en memoria
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
